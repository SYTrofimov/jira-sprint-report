# Claude Code entry point

Read [`AGENTS.md`](AGENTS.md) first — it is the runtime entry point for agent instructions in this
repo, and it points to the rules table of contents in [`agents/rules/`](agents/rules/).

This file exists only so those instructions load automatically at session start. Keep project
instructions in `AGENTS.md`; do not duplicate them here. Only Claude Code harness specifics — things
that would not apply to another agent runtime — belong below.

## Harness notes

None yet.
